"""
AccuMark 批次匯出的核心邏輯。

分層原則（design.md TD-3）：這個套件裡除了 uia.py 之外，全部是純函式——
不碰檔案系統、不碰 UI、不看時間。UI 自動化程式碼在沒有 AccuMark 的機器上
根本跑不起來，若讓流程判斷混在裡面，整個專案就只能靠在目標機上試錯。

真正容易出錯的地方不在 UI 呼叫，而在流程判斷：檔案算不算寫完、任務該不該
跳過、同名時該叫什麼、這個對話框在不在白名單。這些全是純運算，全都可以測，
而且全都是出錯會造成資料遺失的地方。
"""
